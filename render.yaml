services:
  - type: web
    name: israel-property-advisors
    runtime: node
    buildCommand: echo "No build step needed"
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
